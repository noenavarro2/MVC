@using MVCInicial.Models;

@{
    ViewBag.Title = "Desplegable";
}

<h2>Desplegable</h2>

@Html.DropDownList("LasMarcas", new List<SelectListItem>{ 
    new SelectListItem{Text = "Opel", Value="1", Selected=true},
    new SelectListItem{Text = "Ferrari", Value="2"},
    new SelectListItem{Text = "Mercedes", Value="3"}

}, "Selecciona Marca")

@Html.DropDownList("Marcas", "Selecciona Marca ole ole");

<select id="narcos" name="narcos">

    @foreach(MarcaModelo m in ViewBag.Marcas1)
    {
        <option value="@m.ID">@m.Nom_marca</option>
    }
</select>